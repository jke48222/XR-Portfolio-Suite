using UnityEngine;

namespace MRPocketPortals
{
    public sealed class AppEntry_MRPocketPortals : MonoBehaviour
    {
        private void Start()
        {
            Debug.Log("[MRPocketPortals] AppEntry started. Add app-specific wiring here later.");
        }
    }
}
